建造者模式：主要用来创建一些比较复杂的对象，这些对象的创建过程比较稳定，但是对象的创建细节不一样。

建造者模式组成：
指挥者（用来组织具体的创建过程），可以将指挥者抽象为抽象类，不同的具体指挥者去实现这个抽象类，这样的话，可以改变具体的创建过程。
具体指挥者类
建造者过程(这个可以抽象为抽象类或者接口)，定义了创建一个对象的所有步骤
具体建造者类（实现建造者过程），但是每个建造者类有自己的不同的对象细节处理。

适用场景：
适用于创建过程稳定，但是对象创建细节不一致的场景。
