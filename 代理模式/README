代理模式：为其他对象（代理背后的真实对象）提供一种代理以控制对目标对象(真实对象希望访问的对象)的访问。
代理模式情况下，真正发送请求和最后希望得到结果的对象都是真实对象（小飞机情况下，可以理解成我们的大陆IP地址），但是代理类也是可以做各种操作的
（小飞机情况下，国内IP其实是不能访问国外的，但是当我们使用代理翻墙的时候真正一个请求是从大陆开始的，请求的返回也是最终要返回到大陆IP的），
这种情况，代理（香港IP）就在整个代理过程中做了一些自己特殊的操作。

代理模式的应用：
小飞机（翻墙）：
我们在国内其实就是真实对象
使用的代理（假设在香港）
访问谷歌（我们真实想要访问的对象，但是我们自己不能直接访问，可以通过代理的模式去访问）